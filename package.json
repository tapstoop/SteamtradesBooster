{
  "name": "steamtrades-price-tracker",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "mkdir -p dist/background && esbuild content/content.js --bundle --outfile=dist/content.js --format=iife && cp background/*.js dist/background/ && cp content/ggdeals-scraper.js dist/ggdeals-scraper.js",
    "build:watch": "esbuild content/content.js --bundle --outfile=dist/content.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
