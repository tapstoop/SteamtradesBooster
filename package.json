{
  "name": "steamtrades-price-tracker",
  "version": "0.1.2",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "node build.js chrome",
    "build:chrome": "node build.js chrome",
    "build:firefox": "node build.js firefox",
    "build:watch": "esbuild content/content.js --bundle --outfile=dist/content.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.27.4",
    "jsdom": "^29.1.1",
    "vitest": "^2.0.0"
  },
  "dependencies": {
    "playwright": "^1.59.1"
  }
}
